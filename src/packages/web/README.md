# Web app

A standard React web page built with plain JavaScript, using Vite as the build tool.

It acts as IO for the Backend.

The Frontend has its own local storage, where it caches some of the details.
It also uses local storage to store user settings.